# Dependências do Sistema AutoPost
# Sistema de geração automática de conteúdo técnico com IA

# Core dependencies
google-generativeai>=0.3.0
python-dotenv>=1.0.0
requests>=2.31.0

# Standard library extensions (já incluídas no Python, mas listadas para referência)
# pathlib - manipulação de caminhos de arquivo
# datetime - manipulação de datas e horários  
# json - processamento de JSON
# re - expressões regulares
# random - geração de números aleatórios
# hashlib - funções de hash
# subprocess - execução de comandos do sistema
# typing - type hints
# os - interação com sistema operacional
# sys - parâmetros e funções específicas do sistema

# Dependências opcionais para desenvolvimento
# pytest>=7.0.0  # Para testes unitários
# black>=23.0.0  # Para formatação de código
# flake8>=6.0.0  # Para linting
# mypy>=1.0.0    # Para verificação de tipos

# Dependências do sistema (não Python)
# git - para controle de versão e commits automáticos
# hugo - para geração do site estático (se aplicável)

# Notas de instalação:
# 1. Instale as dependências: pip install -r requirements.txt
# 2. Configure as variáveis de ambiente no arquivo .env
# 3. Configure a API key do Google Generative AI
# 4. Certifique-se de que o git está configurado (user.name, user.email)